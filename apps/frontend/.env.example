# API
VITE_API_URL=http://localhost:3001/api
VITE_WEBSOCKET_URL=http://localhost:3001

# UploadThing CDN (leave empty to use local storage fallback URLs)
VITE_UPLOADTHING_APP_ID=m80xbwtvud

# Firebase (Push Notifications)
# NOTE: Firebase is used on the backend, not the frontend
# The backend uses Firebase Admin SDK for push notifications
# No Firebase credentials are needed in the frontend .env files
# To configure push notifications, set backend credentials in apps/backend/.env.example

